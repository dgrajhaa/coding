apply plugin: "war"

bootWar {
    mainClass = "com.inn.ticket.reservation.BookingApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
